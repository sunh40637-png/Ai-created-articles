# 历史记录持久化统一处理逻辑

## 1. 当前真实方案

当前项目里的文章创作历史，不再只是浏览器本地数据。

现在统一采用“三层持久化”：

1. 浏览器本地 `localStorage`
   - 仍由 `zustand persist` 负责
   - 当前 key：`content-creation-sessions-v1`
2. 本机文件镜像
   - 路径：`.local-data/content-creation-sessions.json`
3. 阿里云 OSS 云端镜像
   - 根目录前缀：`content-system/`
   - 当前至少会同步：
     - `content-system/sessions/*.json`
     - `content-system/articles/*.json`
     - `content-system/index/sessions.json`
     - `content-system/index/articles.json`
     - `content-system/topic-library/topic-library.json`
     - `content-system/index/topics.json`
     - `content-system/configs/writing-config.json`
     - `content-system/configs/system-config.json`
     - `content-system/index/configs.json`

这意味着：

- 历史记录已经被当成“用户数据”处理
- Git 不再是数据同步手段
- 同一台电脑上，重启浏览器、重启项目后，历史应当还能恢复
- 不同电脑如果接入同一个 OSS Bucket，也应该能恢复同一份历史

另外，当前项目级运行配置已经改成：

- 根目录 `runtime-config.shared.json` 进入 Git 跟踪
- 双机切换时默认靠这份文件同步 `MiniMax / OSS / Doubao` 配置
- `.env` 退化为本机临时覆盖，不再是跨设备协作的前提

## 2. 当前读取与写入口径

### 2.1 读取规则

当前页面通过 `/api/content-sessions` 读取历史数据。

服务端读取顺序是：

1. 先读本机镜像 `.local-data/content-creation-sessions.json`
2. 如果 OSS 已配置，再读 OSS 中的会话数据
3. 比较本地与云端的 `updatedAt`
4. 谁更新，就用谁恢复

也就是说：

- 不是无脑“以本地为准”
- 也不是无脑“以云端为准”
- 当前是“择新恢复”

### 2.2 写入规则

当前服务端写入顺序是：

1. 先写本机镜像
2. 再尝试写 OSS 云端镜像

如果 OSS 暂时不可用：

- 本地写入仍然成功
- 页面不会直接丢历史

## 3. 为什么之前会误以为是 Git 导致丢失

历史上这块之所以容易出问题，是因为项目最早只依赖：

- `localStorage`

而浏览器本地存储天然绑定：

- 当前电脑
- 当前浏览器
- 当前站点 origin

所以以前看起来像是：

- `git pull` 后历史没了

但更准确地说，其实是：

- 当时历史没有真正作为独立用户数据来持久化

现在这点已经修正了。

## 4. 当前方案能做到什么

### 当前已经做到

1. 同一台电脑上，重启浏览器后仍能恢复
2. 同一台电脑上，`git pull` 后不应该自动清空历史
3. 不同电脑在接入同一个 OSS 配置后，可以从云端恢复历史
4. 当前会话和文章状态已经能同步到 OSS

### 当前还没做到的口径边界

1. `选题库` 和 `配置` 虽然已经同步到 OSS，但页面当前仍以代码内常量为运行时来源
2. 还没有做“账号登录体系”
3. 还没有做“多人并发编辑”或冲突合并
4. 还没有做数据库级事务

所以当前方案的定位应该是：

- 轻量云端持久化方案
- 不是正式数据库方案

## 5. 必须遵守的统一原则

从现在开始，后续任何 AI 或开发者处理这块，都必须遵守：

1. 历史记录属于用户数据，不属于 UI 临时状态
2. `git pull`、`npm install`、`npm run dev`、`npm run build` 都不能主动清空历史
3. 不能在页面初始化、mock 切换、功能开关切换时偷偷调用 `resetAllSessions()`
4. 不能随意修改持久化 key
   - 当前 key：`content-creation-sessions-v1`
5. `.local-data/` 必须继续保持在 `.gitignore`
6. 不能把真实用户历史写进 Git 跟踪文件
7. 不能让更旧的本地缓存反向覆盖更近的 OSS 数据
8. 如果要改 schema，必须保留旧数据并做迁移

## 6. 禁止事项

下面这些做法，后续都不要再做：

1. 为了“从头测试”，在页面启动时加一次性清空逻辑
2. 因为 mock 数据切换，就自动重置全部会话
3. 因为 `persist version` 升级，就直接丢弃旧数据
4. 因为“当前拿不到数据”，就用一个新初始会话覆盖旧会话
5. 为了让仓库“看起来更干净”，删除 `.local-data/content-creation-sessions.json`
6. 在没有迁移的情况下，直接修改 OSS 文件路径、索引文件名或主存储 key

## 7. 与 Git 推送 / 拉取相关的统一要求

### 7.1 `git pull`

- `git pull` 不能成为清空历史记录的理由
- 如果拉取后发现历史记录消失，优先排查：
  - 初始化逻辑
  - `persist` key
  - `.local-data` 是否还在
  - OSS 读取逻辑是否报错
- 如果拉取后页面显示为空，不能直接生成新的空白会话覆盖旧数据，必须先确认：
  - `/api/content-sessions` 返回的是空，还是读取失败
  - 本机镜像和 OSS 云端哪一份更新
  - `content-system/index/sessions.json` 是否仍可读
- 不能直接用一个空白会话覆盖现有历史

### 7.2 `git push`

如果本轮动过持久化逻辑，推送前必须补查：

1. `content-creation-sessions-v1` 是否被改动
2. `.local-data/` 是否仍在 `.gitignore`
3. 页面初始化时是否又引入 reset
4. OSS 目录结构和索引路径是否被误改
5. `npm run build` 是否通过
6. `/api/content-sessions` 是否还能正常读写
7. 当前会话写入后，是否仍会同步到 OSS

另外必须补一条：

- Git 只同步代码，不同步用户真实创作数据
- 任何 `.env`、OSS 导出 JSON、`.local-data/` 都不允许进 Git
- 当前私人仓库场景下，项目级密钥允许集中写入 `runtime-config.shared.json`
- 除了 `runtime-config.shared.json` 之外，不要再把 `AccessKey` 分散写进其他 Git 跟踪文件

## 8. 持久化结构变更规则

如果后续要改持久化结构，必须优先满足：

1. 旧数据还能继续读
2. 新旧结构至少能并存一段时间
3. 不能因为换路径、换 key、换字段名就让用户历史直接失效

尤其是下面这些内容，不能直接硬改：

- `content-creation-sessions-v1`
- `content-system/` 根路径
- `index/sessions.json`
- `index/articles.json`
- `index/topics.json`
- `index/configs.json`
- `sessions/*.json`
- `articles/*.json`
- `topic-library/topic-library.json`
- `configs/writing-config.json`
- `configs/system-config.json`

如果确实要改，必须先做：

1. 迁移方案
2. 回退方案
3. 拉取后恢复验证

## 9. 对另一个 AI 的执行要求

如果另一个 AI 继续改这块，执行顺序必须是：

1. 先确认本地镜像机制仍然存在
2. 再确认 OSS 读取和写入仍然存在
3. 再检查有没有新的初始化清空逻辑被引入
4. 如果要改持久化方案，先保留旧数据，再做迁移
5. 不允许为了测试方便自动清空

## 10. 当前结论

这件事的统一结论是：

1. 历史记录已经不是单纯的浏览器临时状态
2. 当前采用 `localStorage + .local-data + OSS` 的三层方案
3. Git 不负责同步用户历史数据
4. 当前真正负责跨设备恢复的是 OSS
5. 后续任何改动，都要以“不能丢用户数据”为最高优先级
