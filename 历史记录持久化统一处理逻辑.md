# 历史记录持久化统一处理逻辑

## 1. 这个问题的根因

当前项目里的文章创作记录，存储方式是：

- 使用 `zustand persist`
- 存到浏览器本地 `localStorage`
- 当前 key 是：`content-creation-sessions-v1`

这意味着：

1. 这份数据是“浏览器本地数据”，不是“项目数据”
2. 它天然和下面这些维度绑定：
   - 当前电脑
   - 当前浏览器
   - 当前站点 origin（例如 `http://localhost:5173`）
3. `git pull` 本身不会同步或恢复这份数据
4. 换一台电脑、换一个浏览器、换一个本地端口，本来就可能看不到旧记录

所以，这个问题不能再被表述成“Git 把记录清掉了”。  
更准确地说，是：

- 现在的历史记录没有作为“用户数据”独立持久化
- 而是寄存在浏览器本地存储里
- 浏览器本地存储对开发环境非常脆弱

## 2. 统一原则

从现在开始，**文章创作历史记录属于用户数据，不属于代码运行时临时状态**。

必须遵守下面这几条：

1. 任何版本更新、`git pull`、`npm install`、`npm run dev`、`npm run build`，都**不能主动清空**历史记录。
2. 不能在页面初始化、mock 切换、功能开关切换时，偷偷调用 `resetAllSessions()`。
3. 不能因为 schema 变更就直接丢掉旧数据，必须走“保留旧数据的迁移”。
4. 不能随意修改持久化 key。
   - 当前 key：`content-creation-sessions-v1`
   - 如果一定要换 key，必须先做旧 key -> 新 key 的迁移
5. 历史记录不能依赖 repo tracked 文件。
   - 不能把真实用户历史直接写进会被 Git 管理的文件里
   - 否则会造成冲突、误提交、跨分支污染

## 3. 禁止事项

下面这些做法，后续任何 AI 都**不要再做**：

1. 为了“从头测试”，在页面启动时加一次性清空逻辑。
2. 因为 mock 数据切换，就自动重置全部会话。
3. 因为 `persist version` 升级，就直接丢弃旧数据。
4. 因为“当前拿不到数据”，就用一个新初始会话覆盖旧会话。
5. 把历史记录继续只当成“UI 状态”，而不是“用户数据”。

## 4. 当前项目的统一处理口径

另一个 AI 如果要继续处理这个问题，必须先按下面这套口径理解：

### 4.1 当前已执行方案

当前项目已经改成“双保险”：

1. 浏览器端仍保留现有 `localStorage` 持久化
   - persist key 仍然是 `content-creation-sessions-v1`
2. 同时增加一份本地文件镜像：
   - `.local-data/content-creation-sessions.json`
3. 页面启动时会尝试读取本地文件镜像
4. 如果当前浏览器里没有有效历史，而本地文件里有，就从本地文件恢复
5. 运行过程中，会把最新会话状态持续镜像回本地文件

这套方案的目的：

1. 保留现有前端同步体验
2. 避免只依赖浏览器 `localStorage`
3. 保证同一台电脑上，`git pull` 后仍能恢复历史记录
4. 允许同一台电脑上更换浏览器或切换本地端口后，从本地文件恢复历史

### 4.2 这套方案的约束

1. `.local-data/` 必须继续保持在 `.gitignore` 中
2. persist key 不能随意改，当前仍是：
   - `content-creation-sessions-v1`
3. `migrate()` 只能做保留式迁移，不能借迁移清空数据
4. 启动恢复必须遵守“择优恢复”：
   - 若本地文件比当前浏览器状态更新，才用本地文件覆盖
   - 不能用更旧的本地文件反向覆盖更近的新记录
5. 如果需要真正“清空历史”，必须由用户显式触发，不能在初始化时做

## 5. 多设备这件事要说清楚

必须统一口径：

### 当前方案做不到

如果只是：

- `localStorage`
- 或者单机本地 JSON 文件

那么它都只能保证**单机本地保留**，不能天然跨电脑同步。

也就是说：

1. 同一台电脑上，应该能保留
2. 另一台电脑上，不应该默认期待自动同步

### 如果以后要跨设备同步

那就不能只靠浏览器或本地文件，必须上“共享存储”方案，例如：

1. 独立数据库
2. 云端账号体系
3. 用户显式导出 / 导入历史记录
4. 某种外部同步目录

在没有这些能力前，**不要把“跨电脑自动同步”当成当前版本的承诺**。

## 6. 对另一个 AI 的执行要求

如果另一个 AI 要继续改这块，必须遵守下面的执行顺序：

1. 先确认本地文件镜像机制是否仍然存在
2. 先检查有没有新的初始化 reset 逻辑被引入
3. 如果要改持久化方案，先保留当前 `.local-data` 数据，再做迁移
4. 不允许直接把旧历史判定为“无效”然后覆盖
5. 不允许为了测试方便自动清空

## 7. 与 Git 推送 / 拉取相关的统一要求

1. `git pull` 不能成为清空历史记录的理由
2. `git push` 前如果本轮动过持久化逻辑，必须补查：
   - persist key 是否被改动
   - 初始化时是否引入了 reset
   - `.local-data/` 是否仍在 `.gitignore`
   - 本地文件镜像恢复逻辑是否仍可用
3. 不允许把 `.local-data/content-creation-sessions.json` 纳入 Git 跟踪
4. 不允许为了“让仓库更干净”而删除用户本地历史镜像文件
5. 如果拉取后发现历史记录消失，优先排查恢复链路，而不是直接生成全新空会话覆盖

## 8. 当前结论

这件事的统一结论是：

1. **历史记录属于用户数据**
2. **用户数据不能被页面初始化或版本更新自动清空**
3. **当前采用 localStorage + 本地文件镜像的双保险方案**
4. **同一台电脑上，git pull 后历史记录应该仍能恢复**
5. **在没有共享存储前，不承诺跨设备自动同步**

后续任何 AI 如果处理这块，都必须以这份文档为准。
