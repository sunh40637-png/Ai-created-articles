# 开发日志

## 2026-03-30

### 本次目标

- 完成内容创作工作台的前端结构与交互迭代
- 增加 AI 选题重推能力
- 调整侧边栏框架与头像显示
- 收紧历史记录展示规则
- 补充项目开发规则文档

### 本次完成内容

- 接入 `MiniMax-M2.7` 相关内容生成与选题重推链路
- 增加 `/api/topic-recommendations` 接口与服务端逻辑
- 完成内容创作页的选题推荐、按偏好重推、我有题目等交互
- 调整内容创作默认页与排版预览结构
- 新增侧边栏 `选题库` 模块入口
- 将侧边栏头像替换为指定图片资源
- 去掉图标 hover 的名称说明遮罩
- 把历史记录收紧为仅展示真实发生过交互的会话
- 补充根目录 `AI开发手册.md`

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `src/stores/useBenchmarkStore.js`
- `vite.config.js`
- `api/topic-recommendations.js`
- `server/topicRecommendations.js`
- `public/sidebar-avatar.png`
- `AI开发手册.md`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 暂不继续扩展新功能
- 暂不主动推进后端新增能力
- 等待用户下一步明确指令

### 下一步

- 当前无默认排期
- 等待用户指令

## 2026-03-30（补充）

### 本次目标

- 仅聚焦 `选题库` 的 UI 结构和列表展示

### 本次完成内容

- 将 `选题库` 主区域从空白占位改为实际列表页
- 预置 `30` 个静态选题卡片
- 卡片样式与内容创作页推荐选题卡保持同一视觉语言
- 布局调整为一排 `3` 个卡片，左右留白收紧
- 当前明确不补任何联动逻辑，只做 UI 展示

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 暂不补“已创作”标注逻辑
- 暂不补选题库与内容创作的跳转或状态联动
- 等待用户下一步指令

## 2026-03-30（再次补充）

### 本次目标

- 重做内容创作首页的预设选题交互
- 删除“我有题目”和“按偏好重推”入口与逻辑
- 增加类型筛选和分页器

### 本次完成内容

- 内容创作首页默认改为从选题库随机加载 `6` 个预设选题
- 删除“我有题目”和“按偏好重推”的页面入口与交互逻辑
- 新增 `筛选` 按钮和悬浮弹窗
- 筛选项固定为 `A型 / B型 / C型`
- 最多可选 `3` 个类型
- 点击确定后刷新当前预设选题
- 筛选生效时按钮高亮，并在右侧显示关闭图标用于退出筛选
- 隐藏“换一批”
- 新增预设选题分页器，单页保持 `6` 个选题

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `src/stores/useBenchmarkStore.js`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前只做 UI 和前端交互
- 暂不补“已创作”状态标注
- 暂不补选题库与内容创作的真实状态联动

## 2026-03-31

### 本次目标

- 调整右侧内容创作工作台里“文字稿”区域的标签信息

### 本次完成内容

- 将右侧文字稿顶部的时间标签替换为作者名称
- 新增文章类型标签，显示当前文章所属类型
- 文字稿顶部标签现在调整为：`版本 / 字数 / 作者 / 类型`

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前只调整 UI 展示，不改动生成逻辑

## 2026-03-31（补充）

### 本次目标

- 修复折叠态历史悬浮卡片的 hover 体验
- 去掉历史记录里的“当前”文字标注

### 本次完成内容

- 调整历史悬浮卡片与图标之间的间距
- 增加透明 hover 过渡区域，避免鼠标移向卡片时弹层消失
- 去掉历史记录项右侧的“当前”文字标注

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前只修复交互和展示问题，不改动历史记录逻辑

## 2026-03-31（再次补充）

### 本次目标

- 去掉文字稿确认区的“确认通过”按钮及对应语义

### 本次完成内容

- 删除文字稿确认区的“确认通过”按钮
- 将推进下一步的主按钮统一为“无需修改”
- 同步清理对应的前端事件参数和提示文案

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前只调整按钮和前端推进文案，不改动正文生成逻辑

## 2026-03-31（排版卡片调整）

### 本次目标

- 统一“排版效果确认”和“文字稿确认”的卡片结构

### 本次完成内容

- 将“排版效果确认”卡片改为与“文字稿确认”一致的两层布局
- 调整为“标题说明 + 浅底信息区 + 操作按钮”的结构
- 重新整理排版确认文案，使其更贴近当前流程语境

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前只调整排版确认卡片的 UI 和文案，不改动排版生成逻辑

## 2026-03-31（预览增强）

### 本次目标

- 为排版预览增加全屏视图
- 增加“复制微信样式”按钮

### 本次完成内容

- 在右侧工作台 tab 栏新增全屏按钮
- 全屏状态使用 `createPortal + fixed inset-0` 覆盖整个页面
- 支持按 `ESC` 退出全屏
- 在排版预览工具栏新增“复制微信样式”按钮
- 增加一套仅使用 inline style 的 Markdown 渲染组件，用于复制到微信公众号编辑器
- 新增隐藏复制源区域，复制时同时写入 `text/html` 和 `text/plain`
- 复制成功后按钮会短暂显示“已复制”

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前只做前端预览增强
- 暂不接入真实公众号编辑器 API

## 2026-03-31（字号文案）

### 本次目标

- 调整排版预览的字号切换文案
- 明确推荐字号

### 本次完成内容

- 将字号切换文案从“小 / 中 / 大”改为“小 / 推荐 / 大”
- 将“推荐”明确对应为 `15px`
- 在预览工具栏右侧补充“推荐字号：15px”提示

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前只调整字号文案和提示，不改动排版生成逻辑

## 2026-03-31（字号统一）

### 本次目标

- 统一可见预览字号和“推荐字号”文案

### 本次完成内容

- 将可见预览字号从旧的 `13 / 14 / 16` 调整为 `14 / 15 / 17`
- 现在“小 / 推荐 / 大”与复制微信样式使用同一组字号
- 修正“推荐字号 15px”与页面实际显示不一致的问题

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前只修正预览字号，不改动其他排版结构

## 2026-03-31（字号标签简化）

### 本次目标

- 简化字号切换 tab 的文案

### 本次完成内容

- 将字号切换按钮文案从“字号小 / 字号推荐 / 字号大”收敛为“小 / 推荐 / 大”

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前只调整按钮文案，不改动字号逻辑

## 2026-03-31（标题字号）

### 本次目标

- 调整排版预览中文章主标题字号

### 本次完成内容

- 将排版预览里的文章主标题字号统一改为 `24px`
- 可见预览与复制微信样式的主标题字号保持一致

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前只调整预览标题字号，不改动其他排版结构

## 2026-03-31（工具栏信息收敛）

### 本次目标

- 删除排版预览工具栏里的冗余说明信息

### 本次完成内容

- 删除“当前查看：PC 预览 / 移动端预览”提示
- 删除“推荐字号：15px”提示
- 工具栏右侧只保留复制相关操作按钮

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前只收敛工具栏展示信息，不改动排版功能

## 2026-03-31（预览工具栏去卡片化）

### 本次目标

- 去掉排版预览工具栏外层卡片感

### 本次完成内容

- 移除排版预览工具栏外层的圆角、边框和白底容器
- 工具栏现在只保留内部控件本身，不再显示独立卡片外框

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前只调整工具栏外观，不改动工具栏功能

## 2026-03-30（MiniMax 配置补充）

### 本次目标

- 检查 MiniMax API 是否真正接入运行时
- 打通根目录 `API_KEY.md` 到本地开发接口和 `api/` 路由的配置读取

### 本次完成内容

- 确认项目原本已有 MiniMax 请求封装与内容创作、选题推荐、分析接口链路
- 发现本地运行时未读取根目录 `API_KEY.md`，接口实际返回 `未配置 MINIMAX_API_KEY`
- 新增统一的 MiniMax 运行时配置读取模块
- 调整 `vite.config.js`，让本地开发接口在缺少 `.env` 时自动回退读取根目录 `API_KEY.md`
- 调整 `api/benchmark-chat`、`api/content-draft`、`api/topic-recommendations`、`api/benchmark-analyze`，统一复用该配置读取逻辑

### 涉及文件

- `server/runtimeConfig.js`
- `vite.config.js`
- `api/benchmark-chat.js`
- `api/content-draft.js`
- `api/topic-recommendations.js`
- `api/benchmark-analyze.js`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过
- 已验证：`POST /api/benchmark-chat`
- 结果：成功返回 `OK`，确认运行时已读取根目录 `API_KEY.md` 中的 MiniMax Key

### 当前约束

- 当前只补配置接入，不扩展新的业务功能
- 仍优先使用环境变量；只有未配置时才回退到根目录文档中的 MiniMax 配置

### 下一步

- 执行构建验证
- 重新启动开发服务并校验 MiniMax 接口返回

## 2026-03-30（副标题宽度微调）

### 本次目标

- 优化内容创作首页标题下方副标题的展示宽度
- 降低副标题在当前桌面视图下频繁换行的情况

### 本次完成内容

- 将内容创作首页标题区文案容器适度加宽
- 将副标题文案做了轻量收短，保持原有含义不变
- 尽量让副标题在常规桌面宽度下保持单行或更稳定的展示

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 仅做标题区局部样式与文案微调
- 不调整整页布局，不改其他模块视觉语言

### 下一步

- 执行构建验证
- 等待用户继续提出下一轮 UI 微调需求

## 2026-03-30（选题卡状态标签）

### 本次目标

- 给选题卡片补充状态标签 UI
- 用颜色区分“已创作”和“创作中”
- 未创作时显示默认状态文案

### 本次完成内容

- 在选题卡片现有标签组最前面新增状态标签
- 绿色标签表示 `已创作`
- 橙色标签表示 `创作中`
- 默认中性标签文案设为 `待创作`
- 当前只做静态 UI 演示，不接入真实业务状态逻辑
- 为保持视觉一致性，同时应用到内容创作页推荐卡片和 `选题库` 列表卡片

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 仅做前端静态展示
- 不新增状态联动，不接真实创作进度

### 下一步

- 执行构建验证
- 等待用户继续提出下一轮 UI 微调需求

## 2026-03-30（选题后 SOP 流程改造）

### 本次目标

- 重做“点击选题后到首版待确认稿输出”这段对话区流程展示
- 去掉独立的“自动流程”卡片包装
- 将流程节点改为贴在 AI 回复后的纵向 SOP 胶囊流

### 本次完成内容

- 移除对话区中的独立“自动流程”卡片
- 将流程状态挂到对应 AI 消息下方，作为消息的一部分展示
- 新增纵向 SOP 胶囊流样式，支持 `waiting / running / done / skipped / failed` 五种状态
- 每个流程节点右侧显示对应耗时或跳过状态，增强用户对处理进度的感知
- 将“选题后生成首版待确认稿”的流程改为 `8` 步：
- `接收选题 / 装载写作要求 / 生成正文初稿 / 发起内容审核 / 生成审核报告 / 判定修订方式 / 执行自动修订 / 输出首版待确认稿`
- 为“文字稿确认后生成排版”“整篇重写”“按意见改稿”“排版确认收束”补充 AI 接管提示文案，并统一改为消息内联流程展示
- 根据当前报告内容推断“执行自动修订”节点是否应显示为 `已跳过`

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前 `8` 步 SOP 仍然是前端定义的产品流程，不是后端逐步回传的实时遥测
- “判定修订方式 / 执行自动修订”目前仍基于现有结果做前端映射，尚未接入完整审核阈值与重写分支逻辑

### 下一步

- 继续根据用户反馈微调节点文案、纵向节奏和视觉层级
- 如需更强真实性，后续再补底层可观测流程状态

## 2026-03-30（选题后静态模拟态）

### 本次目标

- 为“选题后到首版待确认稿”的页面调试补一套静态模拟数据
- 避免每次调整 UI 都必须重新跑真实生成流程

### 本次完成内容

- 新增选题后首稿链路的静态模拟模式开关
- 当前模拟模式开启后，点击任一选题会直接进入首版待确认稿页面
- 默认注入一套完整的虚拟内容：
- 用户选题消息
- AI 接管消息
- 完成态 `8` 步纵向 SOP
- 模拟正文初稿
- 模拟校验报告
- 当前模拟数据默认展示“局部修正后输出首版待确认稿”的结果，便于直接观察完整页面样式

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前为前端静态模拟，不发起真实 `/api/content-draft` 请求
- 该模式仅用于当前 UI 调试，后续联调逻辑时需要关闭模拟开关

### 下一步

- 继续基于静态模拟态调整选题后页面 UI
- 待页面确认后，再恢复真实流程联调

## 2026-03-31（选题后中途状态预览）

### 本次目标

- 将选题后的静态模拟态改为“处理中”的中途状态
- 同时展示 `已完成 / 进行中 / 未开始` 三种 SOP 节点状态
- 优化节点文案，让表达更像产品界面中的常用术语

### 本次完成内容

- 新增首稿链路统一文案常量，收束为：
- `接收选题 / 整理写作要求 / 生成正文初稿 / 开始内容审核 / 输出审核结果 / 判断修改范围 / 自动修订内容 / 准备首版稿件`
- 静态模拟态不再直接展示完整完成态，而是默认停在第 `5` 步“输出审核结果”
- 模拟态现在只保留一条 AI 接管消息，并将纵向 SOP 挂在该消息下方
- 中途状态下不再默认打开右侧工作区，也不注入模拟正文和校验报告
- 将处理中输入框占位文案改为等待提示，避免在模拟态下出现误导性的可编辑暗示
- 未开始节点的圆点图标改为更中性的占位点，和“已跳过”状态区分开

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前中途态仍是前端静态模拟，不会自动推进到完成态
- 真实链路下的“完成提示”仍是第二条 assistant 消息，尚未合并成单消息簇

### 下一步

- 继续按用户反馈微调 SOP 节点文案和纵向节奏
- 如需对齐最终形态，可进一步把真实流程也收成单条 AI 消息簇

## 2026-03-31（流程消息簇合并）

### 本次目标

- 去掉流程回合中的第二条 assistant 完成消息
- 让每个 AI 流程回合只保留一套时间和一个复制按钮

### 本次完成内容

- 为消息更新新增按 `messageId` 定位的更新工具，支持在原消息上补充完成态或失败态文案
- `runFlow` 现在会把 `messageId / flowId / startedAt` 传给回调，便于业务侧直接更新同一条 assistant 消息
- 将“选题生成首稿”“确认文字稿生成排版”“整篇重写”“按意见改稿”“排版确认收束”这几条流程统一改为单消息簇
- 完成态和失败态说明不再追加第二条 assistant 消息，而是直接并入原来的 AI 接管消息
- 静态 mock 与真实流程现在在消息结构上保持一致，避免调 UI 时出现两套形态

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前复制按钮仍只复制消息正文，不包含纵向 SOP 节点文本
- 消息时间保留的是该轮 assistant 消息的创建时间，不是流程完成时间

### 下一步

- 继续根据用户反馈微调节点文案、间距和状态层级
- 如需更强“消息簇”感，可再细调流程区与正文的垂直间距和分组关系

## 2026-03-31（消息簇间距微调）

### 本次目标

- 调整 assistant 消息正文、纵向 SOP 和底部时间区之间的垂直节奏
- 让单条流程消息看起来更像一个整体，而不是三段拼接

### 本次完成内容

- 收紧正文与流程区之间的上间距
- 略微压缩 SOP 节点之间的垂直距离和连接线长度
- 收紧胶囊与耗时之间的横向距离
- 为带 workflow 的 assistant 消息增加更稳定的正文宽度控制
- 将底部时间与复制按钮区和流程区再拉开一点，避免贴得太紧

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 下一步

- 继续根据用户反馈微调节点文案和视觉层级
- 如有需要，可进一步处理完成态补充文案和 SOP 的分组关系

## 2026-03-31（关闭模拟并重置测试态）

### 本次目标

- 关闭选题后页面的静态模拟模式
- 清空之前注入的 mock 会话内容，回到可从头测试的初始状态

### 本次完成内容

- 将选题后的 `CONTENT_FLOW_UI_PREVIEW` 开关关闭，恢复真实流程请求
- 为 store 新增 `resetAllSessions` 动作，用于将内容创作会话恢复为全新初始态
- 在页面初始化时增加一次性重置逻辑：
- 当检测到当前已退出 mock 模式时，会自动把旧会话清空为新的初始会话
- 通过本地标记保证这次重置只执行一次，避免后续正常测试时重复清空数据

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `src/stores/useBenchmarkStore.js`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 本次为“从头测试”做了一次性清空；如果后续还要再次回到全新态，需要手动再清空会话或调整标记

### 下一步

- 开始走真实链路测试
- 根据真实返回结果继续收 UI 和流程文案

## 2026-03-31（首稿链路时间重分配）

### 本次目标

- 修正“首稿链路耗时全部挤在单一节点”的不合理展示
- 补齐最终“呈现给用户”的交付节点

### 本次完成内容

- 将首稿链路第 `8` 步从“准备首版稿件”改为“呈现首版稿件”
- 将“判断修改范围”调整为“判定修改方式”，并保留 `无需修改 / 局部修改` 的结果映射
- 将首稿链路等待中的真实耗时挂回更合理的 AI 写作节点，不再卡在“判定修改方式”
- 在流程完成后，按整轮真实总耗时对首稿链路各步骤进行加权回填
- 当“自动修订内容”被跳过时，会自动剔除该步权重，避免错误分配耗时
- 同步修正了残留 mock 中的首稿节点顺序，保证和真实链路一致

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前各步耗时仍是基于整轮真实总耗时做的前端加权分配，不是后端逐步回传的精确遥测
- “是否修改”的判断目前仍来自现有报告内容映射，不是独立的服务端审核决策节点

### 下一步

- 继续根据真实测试结果微调节点文案和权重分配
- 如需完全真实的节点耗时，后续需要把后端生成链路拆成可观测的阶段性回传

## 2026-03-31（校验报告视图重排）

### 本次目标

- 修正校验报告 tab 的排版拥挤和表格失真问题
- 让 AI 生成的 Markdown 更适合报告型阅读视图

### 本次完成内容

- 为校验报告新增专用 Markdown 组件，不再与正文共用同一套排版样式
- 强化了报告标题、分区、段落、列表、引用和分隔线的层级节奏
- 为 Markdown 表格增加横向滚动容器、最小宽度和更稳定的单元格排版，避免列宽被挤碎
- 将校验报告工作台改为独立的阅读容器，缓解原先内容直接贴边导致的拥挤感
- 在内容生成提示词中补充“报告优先使用标题、列表、表格”的约束，改善后续报告源格式

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `server/contentCreation.js`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前已有历史报告内容不会自动重写格式，只会按新的视图样式重新渲染
- 如果模型返回的 Markdown 层级本身很差，视图会改善观感，但不能完全替代内容侧结构优化

### 下一步

- 继续根据真实报告内容微调表格列宽和段落密度
- 如有需要，可再针对“结论区”做更明确的摘要样式

## 2026-03-31（文字稿阅读视图重排）

### 本次目标

- 修正文字稿 tab 中标题、段落和引用区域的拥挤问题
- 让右侧文字稿更接近长文阅读器，而不是通用 Markdown 内容块

### 本次完成内容

- 为文字稿新增专用 Markdown 组件，不再与聊天消息和普通内容共用同一套排版节奏
- 放大并拉开一级、二级、三级标题层级，避免标题和正文贴得过近
- 提升正文段落字号、行高和段间距，让连续阅读更松弛
- 重排列表、引用、图片和表格样式，减少局部内容堆叠带来的压迫感
- 将文字稿工作台改成独立阅读容器，并补充顶部元信息与正文之间的留白

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前文字稿的排版改善只作用于右侧“文字稿”视图，不会影响聊天消息里的 Markdown 展示
- 如果模型输出的源 Markdown 层级本身非常混乱，阅读器只能缓解拥挤感，不能完全替代内容结构优化

### 下一步

- 继续根据真实首稿内容微调标题上间距和段落密度
- 如有需要，可再补充首段导语或重点引用的特殊样式

## 2026-03-31（首稿流程时间回刷修正）

### 本次目标

- 修正首稿生成流程中“当前节点先累积很长时间，完成后又被回刷变短”的问题
- 让用户在运行过程中看到的节点耗时，和最终保留下来的节点耗时保持一致

### 本次完成内容

- 移除了首稿流程在完成时对整轮节点耗时做二次重分配的逻辑
- 保留运行过程中已经展示过的节点耗时，不再在完成瞬间把正文初稿等节点时间回刷成更小值
- 将首稿流程的等待锚点后移到“输出审核结果”，避免长时间一直卡在“生成正文初稿”
- 为首稿流程单独接入更接近真实等待的节点节奏，不再沿用过于压缩的短动画时间

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前节点耗时仍然是前端按实际等待过程呈现的 UI 时间，不是后端逐步回传的精确遥测
- 如果要做到每个节点都和真实后端阶段一一对应，后续仍需要服务端拆成可观测的阶段性回传

### 下一步

- 继续根据真实测试观察首稿流程的节点停留位置是否还需要微调
- 如果后续要进一步做实，可把生成、审核、修订拆成服务端可上报的独立阶段

## 2026-03-31（首稿链路改为真实多阶段处理）

### 本次目标

- 把“选题后到首版稿件”从前端推演节点改成后端真实多阶段处理
- 让工作流节点状态和耗时来自真实阶段推进，而不是一次性结果返回后的前端映射

### 本次完成内容

- 后端新增首稿多阶段 pipeline，按“写稿 / 审核 / 判定 / 自动修订 / 呈现”顺序真实推进
- `/api/content-draft` 新增流式返回能力，首稿请求会按阶段持续吐出进度事件
- 前端首稿流程改为消费后端实时阶段事件，不再靠本地动画模拟 8 个节点
- “判定修改方式”优先读取后端返回的真实决策值，不再只靠报告文案猜测
- 保留了原有单次 JSON 返回模式，避免影响其余尚未切到流式的链路

### 涉及文件

- `server/contentCreation.js`
- `vite.config.js`
- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过
- 已执行：`runInitialContentPipeline` 真实链路自检
- 结果：成功按阶段输出进度，最终返回正文与校验报告

### 当前约束

- 当前改实的是真正最关键的“首稿生成”链路，改稿和排版确认等后续链路仍保留原有实现
- 若要让全部流程节点都成为真实阶段，后续还需要继续把改稿、重写、排版等链路逐步拆成多阶段 pipeline

### 下一步

- 用真实页面继续观察首稿阶段停留是否符合预期
- 按相同方式逐步把“整篇重写”和“按意见改稿”也改成真实多阶段处理

## 2026-03-31（首稿阶段顺序修正）

### 本次目标

- 修正“整理写作要求”被错误挂长时间的问题
- 让首稿流程的长时间等待落在真正执行 AI 任务的节点上

### 本次完成内容

- 调整首稿 pipeline 的阶段推进顺序
- 让“接收选题 / 整理写作要求”快速完成后，再进入“生成正文初稿”
- 让内容审核阶段的真实等待落在“输出审核结果”，而不是前置的准备节点
- 避免首稿生成完成后重复推进前序节点，导致上一个节点耗时被错误拉长

### 涉及文件

- `server/contentCreation.js`
- `开发日志.md`

### 验证情况

- 已执行：`runInitialContentPipeline` 真实链路自检
- 结果：阶段推进顺序符合预期
- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前阶段耗时已经挂到更合理的节点上，但要做到 100% 与模型内部推理步骤对应，仍需要更细粒度的服务端阶段拆分

### 下一步

- 继续根据真实页面测试观察每个节点的停留位置是否还需要微调

## 2026-03-31（确认卡片信息结构精简）

### 本次目标

- 调整“文字稿确认”和“排版效果确认”的信息层级
- 去掉标题区和操作卡之间的重复说明

### 本次完成内容

- 去掉“文字稿确认”标题下方的重复说明
- 将“文字稿确认”卡片改成状态提示 + 操作按钮，不再重复提问
- 去掉“排版效果确认”标题下方的重复说明
- 将“排版效果确认”卡片改成状态提示 + 操作按钮，保留最小必要提示

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前仅调整确认卡片的信息结构，没有改动流程逻辑和按钮动作

### 下一步

- 继续根据实际观感决定是否进一步收紧卡片留白和层级

## 2026-03-31（历史记录持久化修复）

### 本次目标

- 解决拉取代码后本地历史记录被自动清空的问题
- 保留现有 `localStorage` 会话数据，不再触发一次性重置

### 本次完成内容

- 移除页面初始化时的 `resetAllSessions()` 自动重置逻辑
- 保留现有 Zustand 持久化 key 和版本迁移，不再在页面加载时额外清空历史

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 已经被清空的旧历史无法自动恢复，本次修复只防止后续再次被初始化逻辑清掉

### 下一步

- 继续观察后续拉取代码后的本地历史是否稳定保留

## 2026-03-31（写作规则 A/B 测试接入）

### 本次目标

- 将当前稳定版规则和候选版规则并存管理
- 支持按规则版本运行写作与审核链路
- 产出一套可直接执行的测试流程

### 本次完成内容

- 新增 `server/contentRuleProfiles.js`，将写作与审核规则拆为 `A / B` 两套配置
- 保留 `A` 作为默认稳定版，不直接覆盖现有规则
- 将内容生成、审核、修订、首稿多阶段 pipeline 全部接入 `ruleProfileId`
- 开发环境接口和正式接口都支持传入 `ruleProfileId`
- 新增固定测试题单 `content-style/ab-test-topics.json`
- 新增测试脚本 `scripts/run-rule-ab-test.mjs`
- 新增测试说明文档 `规则AB测试流程.md`

### 涉及文件

- `server/contentRuleProfiles.js`
- `server/contentCreation.js`
- `api/content-draft.js`
- `vite.config.js`
- `content-style/ab-test-topics.json`
- `scripts/run-rule-ab-test.mjs`
- `package.json`
- `规则AB测试流程.md`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前前端工作台仍默认走 `规则A`
- 暂未增加前端可视化 A/B 切换开关，测试建议优先使用脚本执行

### 下一步

- 等待用户按测试流程执行 A/B 对照
- 根据测试结果决定是否继续迭代 `规则B` 或回退保持 `规则A`

## 2026-03-31（生效规则切换与文档整理）

### 本次目标

- 按最新决策将当前生效规则切换为“B型最新版，A/C型原版”
- 将当前实际生效的提示词规则整理为可阅读文档

### 本次完成内容

- 将默认规则从单一 `A` 切换为混合生效版
- 当前实际生效逻辑改为：`B型 -> 规则B`，`A型 / C型 -> 规则A`
- 保留 A/B 测试能力，显式指定规则版本时仍可单独跑 `A` 或 `B`
- 新增根目录 `文案创作规则` 文件夹
- 将当前实际生效的提示词规则按通用规则、类型规则、笔名规则、审核规则拆为多个 Markdown 文件
- 同步更新 `规则AB测试流程.md`，将默认说明改为混合生效版

### 涉及文件

- `server/contentRuleProfiles.js`
- `server/contentCreation.js`
- `api/content-draft.js`
- `vite.config.js`
- `文案创作规则/00-当前生效说明.md`
- `文案创作规则/01-通用写作硬性规则.md`
- `文案创作规则/02-A型提示词规则（当前生效）.md`
- `文案创作规则/03-B型提示词规则（当前生效）.md`
- `文案创作规则/04-C型提示词规则（当前生效）.md`
- `文案创作规则/05-笔名风格提示词.md`
- `文案创作规则/06-审核与修订提示词规则.md`
- `规则AB测试流程.md`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前前端界面没有直接展示“当前命中的规则版本”
- 当前文档是对现行提示词规则的人工整理，不是运行时自动导出的镜像

### 下一步

- 等待用户检查 `文案创作规则` 文件夹内容
- 如需继续调整，再按文件维度逐项细化

## 2026-03-31（规则更新说明 v1.1 同步）

### 本次目标

- 按《规则更新说明 v1.1》同步写作、审核与修订规则
- 不新增独立 `PROFILE_C`，继续让 `C型` 走 `PROFILE_A`
- 只通过 prompt / 规则层落地，不新增程序化硬约束

### 本次完成内容

- 在 `规则A` 和 `规则B` 中同步补齐古文真实性核查、真实人物使用规范、禁止虚构著作、同一古籍每篇最多引用一次、每篇至少两个不同人物或案例
- 放开 `A型` 开篇方式，允许名人名言、无署名格言、场景切入三种变体
- 将段落开头方式多样化统一收口为建议级约束，只写入审核报告和可优化建议，不单独影响判定
- 扩展审核报告结构，新增判定理由、人物与案例检查、开篇与段落变体检查、著作真实性说明等要求
- 扩展自动修订提示词，明确存疑古文替换、真实人物改虚构人物、重复古籍替换、单一人物补第二案例等动作
- 同步更新 `文案创作规则` 目录下的规则文档和 `规则AB测试流程.md`

### 涉及文件

- `server/contentRuleProfiles.js`
- `文案创作规则/00-当前生效说明.md`
- `文案创作规则/01-通用写作硬性规则.md`
- `文案创作规则/02-A型提示词规则（当前生效）.md`
- `文案创作规则/03-B型提示词规则（当前生效）.md`
- `文案创作规则/04-C型提示词规则（当前生效）.md`
- `文案创作规则/06-审核与修订提示词规则.md`
- `规则AB测试流程.md`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 本次没有新增程序化硬校验，真实性、人物规范、著作真实性等仍主要依赖模型按提示词执行
- `C型` 仍未拆出独立 `PROFILE_C`，当前继续继承 `规则A`

### 下一步

- 用真实选题继续观察审核报告对新增栏目和判定规则的执行质量
- 如后续发现模型经常漏判，再考虑把高风险项升级为程序化校验

## 2026-03-31（候选标题并入首版稿件）

### 本次目标

- 按《标题生成规范》接入候选标题能力
- 不新增独立流程节点，不改现有 SOP 展示
- 让候选标题和正文一起返回，并在右侧文字稿顶部展示

### 本次完成内容

- 后端首稿链路和改稿链路新增 `titleCandidates` 返回字段
- 标题生成不再单独起一次新的创作流程，而是嵌入审核/修订返回中，基于最终正文内容反推 `5` 个候选标题
- 扩展内容创作、审核、修订提示词，明确标题字数、公式覆盖、禁用规则和 JSON 输出结构
- 右侧 `文字稿` 视图新增“候选标题”区块，显示在正文上方
- 每个候选标题支持手动删除，至少保留 `1` 个
- 当候选标题删到只剩 `1` 个时，该标题会同步成为当前会话标题，并作为后续排版预览使用的正式标题
- 排版预览和微信复制预览的主标题改为优先读取当前保留标题；未收敛到单标题时仍回退到原始选题标题

### 涉及文件

- `server/contentCreation.js`
- `server/contentRuleProfiles.js`
- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前没有新增独立“标题生成”流程节点，标题生成是随正文创作结果一起返回的
- 当前不会强制用户在进入排版前删到只剩 `1` 个标题，但只有收敛到单标题后，排版预览才会使用该标题作为正式标题

### 下一步

- 用真实选题验证标题质量、删除交互和排版标题同步是否符合预期
- 如后续需要，再决定是否增加“重新生成候选标题”或更严格的排版前校验

## 2026-03-31（候选标题收回为单标题方案）

### 本次目标

- 将“5 个候选标题 + 删除收敛”的复杂交互收回为“1 个正式标题”
- 保持现有 SOP 节点不变
- 继续让标题基于最终正文内容生成，并直接用于右侧文字稿和后续排版

### 本次完成内容

- 后端标题返回字段从 `titleCandidates` 收敛为 `generatedTitle`
- 内容创作、审核、修订提示词同步改为“只生成 1 个正式标题”，不再要求返回 5 个候选标题结构
- 后端解析层保留了对旧 `titleCandidates` 的兼容读取，避免模型偶发返回旧结构时直接丢标题
- 右侧 `文字稿` 视图移除了候选标题列表和删除操作，改为在正文上方展示单个“正式标题”区块
- 会话标题、排版预览标题和微信复制预览标题都改为优先读取 `generatedTitle`
- 旧会话若仍带有 `titleCandidates`，前端会自动回退读取第一个标题，避免历史数据直接失效

### 涉及文件

- `server/contentCreation.js`
- `server/contentRuleProfiles.js`
- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前没有单独新增“标题生成”流程节点，标题仍然随正文处理结果一起返回
- 当前标题质量仍主要依赖提示词约束，不做程序化强校验

### 下一步

- 用真实选题继续观察单标题质量是否稳定
- 如后续仍出现标题缺失，再决定是否增加标题生成失败重试或显式异常提示

## 2026-04-01（选题库 v1.0 合并导入）

### 本次目标

- 按《选题库_v1.0》更新当前选题库
- 已有重复标题跳过，仅新增当前库里不存在的选题
- 保持项目现有字段结构不变

### 本次完成内容

- 对照当前选题库与导入清单完成标题级去重
- 保留现有 `30` 条选题不动
- 跳过重复标题 `1` 条
- 新增缺失选题 `69` 条
- 当前选题库总数更新为 `99` 条
- 新增条目继续沿用当前项目的字段结构：`id / title / type / penName / reason / theme`
- 为新导入选题补充了与当前项目一致的 `reason` 展示字段

### 涉及文件

- `src/stores/useBenchmarkStore.js`
- `开发日志.md`

### 验证情况

- 已执行：选题库数量与标题去重自检
- 结果：`99` 条，重复标题 `0` 条
- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前按“标题完全一致”判断重复，未做近似标题归并
- 新导入选题的 `reason` 为按主题和类型生成的统一说明，不是逐条人工撰写

### 下一步

- 等待用户检查选题库页面展示效果
- 如需继续导入下一版选题清单，再按同样方式合并

## 2026-04-01（侧边栏新增文章列表模块）

### 本次目标

- 在侧边栏模块区新增 `文章列表`
- 保持现有图标库和侧边栏结构规范不变
- 主区域内容先留空，不补功能和联动

### 本次完成内容

- 在现有模块配置中新增 `文章列表` 入口
- 模块顺序调整为：`选题库` 下方新增 `文章列表`
- 主区域增加 `文章列表` 对应的空白模块画布
- 保持 `新建`、`选题库`、`AI 对话历史` 的现有交互不变

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- `文章列表` 当前仅补侧边栏入口和空白页面
- 暂未添加列表内容、筛选、状态或联动逻辑

### 下一步

- 等待用户继续定义 `文章列表` 的 UI 和交互细节

## 2026-04-01（文章列表模块 v1）

### 本次目标

- 将 `文章列表` 从空白模块升级为真实的前端列表页
- 只聚合已确认文字稿及之后阶段的文章
- 补右侧抽屉预览，但不跳转 AI 对话、不补发布能力

### 本次完成内容

- 基于当前 `sessions` 派生文章列表数据，不新增 store 字段
- 过滤规则调整为仅展示 `stageId === preview / completed` 的会话
- 列表按 `updatedAt` 倒序显示，每个 AI 对话只对应一条文章记录
- 列表页面仅保留 `创作中` 分组，不再出现 `已发布`
- 每一行文章信息固定为：标题、状态标签、母题标签
- 状态标签映射为：
  - `preview` => `已确认文字稿`
  - `completed` => `已排版`
- 点击文章行可打开右侧抽屉预览
- 抽屉宽度约为页面的 `70%`
- `已确认文字稿` 文章只展示 `文字稿` Tab
- `已排版` 文章展示 `文字稿` 与 `排版预览` 两个 Tab，默认打开 `排版预览`
- 预览内容复用了现有 `DraftWorkbench` 和 `PreviewWorkbench`

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前不做 `已发布` 状态和分组
- 当前不支持从文章列表跳回 AI 对话
- 当前抽屉只承担预览，不支持编辑

### 下一步

- 等待用户继续定义 `文章列表` 的交互细节

## 2026-04-01（文章列表抽屉预览滚动修复）

### 本次目标

- 调整文章列表抽屉标题文案
- 修复抽屉打开时排版预览无法正常向下滚动的问题
- 修复遮罩层滚动会带动底层页面的问题
- 调整抽屉宽度到更宽的展示比例

### 本次完成内容

- 抽屉标题改为固定文案 `文字预览`
- 将文章标题下移到标题下方作为次级信息展示
- 抽屉宽度由约 `70%` 调整为约 `80%`
- 抽屉打开时锁定 `body/html` 滚动，避免底层页面受滚动影响
- 为抽屉内容区和预览区补充独立滚动与 `overscroll` 约束
- 将 `PreviewWorkbench` 根容器补为 `h-full`，确保抽屉内滚动正常生效

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前抽屉仍只承担预览，不支持编辑
- 当前仍不支持从文章列表跳转回 AI 对话

### 下一步

- 等待用户继续定义抽屉布局和预览细节

## 2026-04-01（文章列表抽屉补全屏按钮）

### 本次目标

- 让文章列表抽屉与 AI 对话右侧工作台保持一致
- 在抽屉的 Tab 区右侧增加全屏图标按钮
- 统一抽屉在全屏状态下的退出行为

### 本次完成内容

- 为文章列表抽屉新增全屏切换按钮
- 抽屉支持在约 `80%` 宽度与全屏宽度之间切换
- 全屏按钮图标与 AI 对话右侧工作台保持一致
- 调整 `Esc` 行为：全屏时先退出全屏，非全屏时关闭抽屉
- 切换到新的文章会话时自动重置抽屉全屏状态

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前抽屉仍只承担预览，不支持编辑
- 当前仍不支持从文章列表跳转回 AI 对话

### 下一步

- 等待用户继续定义抽屉预览细节

## 2026-04-01（新增素材库模块框架）

### 本次目标

- 在侧边栏新增 `素材库` 模块
- 主区域先补一个独立的空状态页面
- 不接图片导入、不接图片列表、不接查看层交互

### 本次完成内容

- 在现有侧边栏模块区新增 `素材库`
- 图标继续沿用 `lucide-react` 体系
- `素材库` 位于 `文章列表` 下方
- 主区域增加 `素材库` 对应的空状态页面
- 页面仅保留标题 `素材库` 和轻量空状态说明
- 未新增导入按钮、素材数量、图片网格或预览弹层

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 当前仅补模块框架和空状态页面
- 当前不接真实图片数据、不做持久化、不做图片查看交互
- 后续素材库仍按“4 列网格 + 黑色遮罩看图层”的方向继续扩展

### 下一步

- 等待用户继续定义素材库图片网格和看图层细节

## 2026-04-01（素材库一期接入与首批素材导入）

### 本次目标

- 把 `素材库` 从空状态模块升级成可用的管理页
- 采用本地目录一次性入库的方式，把图片正式导入仓库
- 补齐素材元数据索引、列表接口、标签编辑和删除记录能力

### 本次完成内容

- 新增共享素材枚举与排序定义，统一 `emotion / topic / figures / sort` 规则
- 新增本地 JSON 索引读写层，索引文件落到 `public/assets/image-library/index.json`
- 新增素材库接口：
  - `GET /api/library-assets`
  - `PATCH /api/library-assets/:id`
  - `DELETE /api/library-assets/:id`
- Vite 开发环境新增素材库中间件，保证本地开发可直接访问这些接口
- `素材库` 页面从占位态升级为真实管理页，支持：
  - 图片网格展示
  - 按 `emotion / topic / figures` 筛选
  - 按 `usedCount / createdAt` 排序
  - 单图编辑标签与场景描述
  - 删除素材记录（不删本地文件）
- 新增一次性导入脚本 `npm run import:library-assets`
- 已将本地目录 `/Users/shh/Downloads/suciaku ` 中的 45 张图片导入到 `public/assets/image-library/`
- 导入时将原始大体积 PNG 转成仓库内的 JPEG 展示图，素材目录总大小约为 `48MB`
- 首批素材标签已在导入阶段人工识别并写入索引，后续可在页面继续调整

### 涉及文件

- `shared/libraryAssets.js`
- `server/libraryAssets.js`
- `api/library-assets.js`
- `api/library-assets/[id].js`
- `vite.config.js`
- `src/pages/BenchmarkWorkbenchPage.jsx`
- `scripts/libraryAssetSeedManifest.mjs`
- `scripts/import-image-library.mjs`
- `public/assets/image-library/`
- `package.json`
- `开发日志.md`

### 验证情况

- 已执行：`npm run import:library-assets -- '/Users/shh/Downloads/suciaku '`
- 结果：成功导入 45 张素材并生成 `index.json`
- 已执行：`npm run build`
- 结果：通过
- 已执行：素材库读写自检
- 结果：列表筛选和同值更新均通过

### 当前约束

- 当前不做产品内上传入口，后续新增素材仍走本地目录导入脚本
- 当前不做排版阶段自动检索配图，`usedCount / lastUsedAt` 仅做字段预留
- 当前图片标签来自导入阶段的一次性识别和人工整理，不在运行时调用视觉模型

### 下一步

- 等待用户继续测试素材库页面的筛选、编辑和删除体验
- 如后续需要，再把素材库与排版阶段的自动检索配图逻辑打通

## 2026-04-01（素材库空列表问题修复）

### 问题定位

- 素材图片与 `index.json` 均已成功导入，但开发环境下素材库页面显示为空
- 根因是 `vite.config.js` 中素材库 dev API 使用了 `server.middlewares.use('/api/library-assets', ...)`
- 中间件挂载后 `req.url` 中的前缀已被剥离，实际命中路径应为 `'/'` 或 `'/:id'`
- 之前仍按完整路径 `'/api/library-assets'` 判断，导致素材库接口逻辑没有真正执行
- 同时前端在读取列表时把非 JSON 响应静默当成空数据，进一步掩盖了问题

### 修复内容

- 修正素材库 dev API 的路径判断逻辑，确保 `GET /api/library-assets`、`PATCH /api/library-assets/:id`、`DELETE /api/library-assets/:id` 能正确命中
- 补充前端响应校验，素材库接口若返回非 JSON 响应，将直接报错而不是默默显示空列表
- 重启本地 `vite` 开发服务，使新的 dev middleware 配置立即生效

### 涉及文件

- `vite.config.js`
- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过
- 本地开发服务已重启，等待浏览器刷新后复测素材库显示

## 2026-04-01（排版自动配图与素材库大图预览）

### 本轮完成

- 排版阶段已接入自动配图，不新增流程节点，仍挂在“确认文字稿 -> 生成排版预览”这段链路里
- 自动配图基于素材库元数据匹配，不调用 AI；每篇固定匹配 3 张图
- 匹配规则已落地：
  - 先取文章母题
  - 不足时补 `通用`
  - 仍不足时扩到全素材库
  - 优先使用 `usedCount` 更少、`lastUsedAt` 更早的图片
  - 强制 `scene` 不重复
  - 强制至少覆盖 2 种 `figures`
- 当前会话的 `imageSelection` 已扩展为真实配图状态，包含：
  - `sourceVersionId`
  - `matchedAt`
  - `referenceAssets`
  - `slots`
- 排版预览与微信复制预览已共用同一套插图逻辑：
  - 3 张图分别插在前三个正文段落后
  - 若正文不足 3 段，剩余图片顺延到最后一个可插入段落后
  - 图片圆角统一为约 `10px`
- 排版确认通过后，会回写素材 `usedCount / lastUsedAt`
- 若素材记录被删除但文章仍引用，预览中会显示“图片已移除”占位
- 素材库页面已补充大图预览：
  - 点击缩略图打开黑色遮罩预览
  - 右上角关闭
  - 右侧中部竖排上下切换
  - 切换顺序跟随当前素材库筛选与排序结果

### 涉及文件

- `server/libraryAssets.js`
- `api/library-assets/match.js`
- `api/library-assets/usage.js`
- `vite.config.js`
- `src/stores/useBenchmarkStore.js`
- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`node -e "import('./server/libraryAssets.js').then(...matchLibraryAssetsForArticle(...))"`
- 结果：可稳定返回 3 张素材，`scene` 不重复，`figures` 至少 2 类
- 已执行：`npm run build`
- 结果：通过

## 2026-04-01（自动配图改为段落画面优先，插图改为段尾锚点）

### 问题修正

- 上一版自动配图更偏向按文章母题筛图，容易出现“对题但不对段落画面”的情况
- 上一版插图锚点也偏向按普通正文段落 `<p>` 处理，导致图片会提前插进某一大段内部，而不是落在该大段结束处

### 本次调整

- 自动配图改为以正文三大段内容为核心输入：
  - 进入排版时，先从当前文字稿里提取 3 个正文大段
  - 每一段都带上 `title + text` 传给后端做配图匹配
- 选图优先级改为：
  - `scene` 画面描述优先
  - `emotion` 情绪次之
  - `figures` 人物构成再次之
  - `topic` 母题只做弱约束与兜底
- 后端匹配支持两层：
  - 优先尝试由 MiniMax 根据三段正文和素材标签做语义选图
  - 若模型不可用或返回不稳定，则退回本地启发式匹配
- 前端插图锚点改为与配图输入保持一致：
  - 优先按正文中的 `## / ###` 小节识别 3 个大段
  - 每张图都插在对应大段的最后一个内容块之后
  - 若正文没有稳定小节结构，才退回按普通正文段落分组
- 排版预览和微信复制预览继续共用同一套插图生成逻辑，保证页面显示和复制结果一致

### 涉及文件

- `server/libraryAssets.js`
- `api/library-assets/match.js`
- `vite.config.js`
- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`node -e "import('./server/libraryAssets.js').then(...matchLibraryAssetsForArticle(...))"`
- 结果：返回的 3 张素材已更贴近段落画面内容，不再主要依赖母题
- 已执行：`npm run build`
- 结果：通过

## 2026-04-02（自动配图段尾锚点修正）

### 问题修正

- 测试发现图片仍有两类位置错误：
  - 会被插到分割线下方，而不是正文段尾与分割线之间
  - 第三张图会错误吃进最后一个无标题结尾，导致被插到收尾段之后

### 本次调整

- 新增对正文分割线块的识别
- 在按 `## / ###` 提取配图段落时：
  - 若当前大段后面存在分割线，则把分割线视为该段结束边界
  - 图片锚点改为分割线前最后一个正文内容块
- 第三段若后面还有一个无标题结尾，则不再把该结尾并入第三个配图段
- 配图输入和插图位置继续共用同一套段落边界，避免“选图按一套、插图按另一套”

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

## 2026-04-02（图片配置模块与排版固定内容接入）

### 本轮完成

- 左侧侧边栏新增 `图片配置` 模块，结构参考 `文章列表`
- 新增全局固定内容配置，采用 `3 图 1 文`：
  - 开头动图
  - 文末固定文案
  - 二维码图片
  - 底部动图
- 新增本地持久化存储目录 `public/assets/fixed-content/`
- 新增全局配置文件 `public/assets/fixed-content/config.json`
- 新增固定内容接口：
  - `GET /api/fixed-layout-config`
  - `PATCH /api/fixed-layout-config`
  - `POST /api/fixed-layout-config/upload`
  - `DELETE /api/fixed-layout-config/asset`
- 本地开发环境已接入对应 dev middleware，支持读取、上传、替换和删除固定内容
- 排版预览已完全替换原先写死的尾部关注区，改为读取全局固定配置
- 微信复制预览和页面可视预览已共用同一套固定内容顺序
- 移动端排版预览改为手机宽度浮层卡片，有圆角和阴影，和背景层级分开

### 涉及文件

- `shared/fixedLayoutConfig.js`
- `server/fixedLayoutConfig.js`
- `api/fixed-layout-config.js`
- `api/fixed-layout-config/upload.js`
- `api/fixed-layout-config/asset.js`
- `vite.config.js`
- `src/pages/BenchmarkWorkbenchPage.jsx`
- `public/assets/fixed-content/config.json`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过
- 已执行：`node -e "import('./server/fixedLayoutConfig.js').then(...readFixedLayoutConfig())"`
- 结果：成功读取默认固定内容配置

## 2026-04-02（历史记录本地文件镜像与恢复）

### 本轮完成

- 保留现有 `zustand persist + localStorage` 方案，继续作为前端即时持久化
- 同时新增一层本地文件镜像：
  - 目录：`.local-data/`
  - 文件：`.local-data/content-creation-sessions.json`
- 页面启动时会尝试读取本地文件镜像
- 如果当前浏览器里没有有效历史，而本地文件里有更完整或更新的记录，就从本地文件恢复
- 运行过程中会把当前最新会话状态持续镜像回本地文件
- `.local-data/` 已加入 `.gitignore`，不会进入 Git 仓库
- 根目录新增统一规则文档，约束后续任何 AI 不得再引入自动清空历史的逻辑

### 涉及文件

- `src/stores/useBenchmarkStore.js`
- `src/pages/BenchmarkWorkbenchPage.jsx`
- `server/contentSessionPersistence.js`
- `api/content-sessions.js`
- `vite.config.js`
- `.gitignore`
- `历史记录持久化统一处理逻辑.md`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

## 2026-04-02（根目录规则文档补充 Git 与历史记录要求）

### 本轮完成

- 更新根目录 `AI开发手册.md`
- 明确补充：
  - 提交与推送要求
  - 拉取代码后的检查要求
  - 文章创作历史记录保存要求
- 更新根目录 `历史记录持久化统一处理逻辑.md`
- 新增与 `git pull / git push` 相关的统一执行口径
- 统一约束后续任何 AI：
  - 不得把历史记录当成可随意清空的 UI 临时状态
  - 不得在拉取或推送前后破坏本地历史镜像恢复链路

### 涉及文件

- `AI开发手册.md`
- `历史记录持久化统一处理逻辑.md`
- `开发日志.md`

### 验证情况

- 文档更新，无需额外构建验证

## 2026-04-02（OSS 云端存储结构设计 v1）

### 本轮完成

- 新增根目录文档 `OSS云端存储结构设计v1.md`
- 基于当前项目真实数据结构，明确第一阶段上 OSS 的目录组织方式
- 明确拆分原则：
  - 一篇文章一个 JSON
  - 一个 AI 会话一个 JSON
  - 选题库一个 JSON
  - 配置单独 JSON
  - 再维护轻量索引文件
- 明确了当前项目字段与云端文件字段的映射关系
- 明确了后续接入时的写入顺序、读取顺序与本地缓存关系
- 明确了后续需要使用的 OSS 环境变量命名

### 涉及文件

- `OSS云端存储结构设计v1.md`
- `开发日志.md`

### 验证情况

- 文档更新，无需额外构建验证

## 2026-04-02（接入阿里云 OSS 作为会话云端同步源）

### 本轮完成

- 新增 `server/ossContentStorage.js`
- 不引入第三方 SDK，直接使用 Node 内置能力实现阿里云 OSS 的基础 JSON 读写
- 现有 `/api/content-sessions` 升级为“双写双读”：
  - 本地仍保留 `.local-data/content-creation-sessions.json`
  - 云端新增 OSS 持久化
- 云端持久化当前会自动派生并写入：
  - `content-system/sessions/*.json`
  - `content-system/articles/*.json`
  - `content-system/index/sessions.json`
  - `content-system/index/articles.json`
- 页面前端接口保持不变，仍通过 `/api/content-sessions` 读写
- 新增 OSS 环境变量到本地 `.env`

### 涉及文件

- `server/ossContentStorage.js`
- `server/contentSessionPersistence.js`
- `api/content-sessions.js`
- `vite.config.js`
- `.env`
- `开发日志.md`

### 验证情况

- 代码已接入，待进行实际 OSS 连通验证
- 已执行：`node -e "import('./server/ossContentStorage.js').then(async (m) => { const result = await m.runAliyunOssHealthcheck(); console.log(JSON.stringify(result, null, 2)); })"`
- 实际结果：未通过
- 原因：阿里云 RAM 子账号当前对目标 Bucket 没有 `oss:PutObject` 权限，返回 `403 AccessDenied`
- 后续已补充：为 RAM 用户授权 `AliyunOSSFullAccess`
- 再次执行 OSS 健康检查：通过
- 再次执行真实会话写入 / 读回校验：通过
- 修复问题：
  - `server/ossContentStorage.js` 中 `buildSessionFilePayload()` 使用了错误变量名 `selectedTopic`
  - 修复后，OSS 已成功写入并可读回当前会话数据

## 2026-04-02（补齐选题库与配置的 OSS 同步）

### 本轮完成

- 在现有 OSS 派生写入基础上，继续补齐：
  - `content-system/topic-library/topic-library.json`
  - `content-system/index/topics.json`
  - `content-system/configs/writing-config.json`
  - `content-system/configs/system-config.json`
  - `content-system/index/configs.json`
- 选题库同步直接复用当前项目内的 `CONTENT_TOPIC_LIBRARY`，保持与页面实际使用的选题一致
- 选题状态同步规则：
  - `pending`：未进入创作
  - `in-progress`：已被会话选中但未完成排版
  - `completed`：已完成排版
- 文案创作配置同步规则：
  - `A型 -> stable`
  - `B型 -> candidate`
  - `C型 -> stable`
- 系统配置同步了当前项目的关键运行参数：
  - `content-creation-sessions-v1`
  - `topicPageSize = 6`
  - `maxArticleSessions = 16`

### 涉及文件

- `server/ossContentStorage.js`
- `src/stores/useBenchmarkStore.js`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 构建结果：通过
- 已执行真实 OSS 重同步，结果通过
- 已确认 OSS 中成功生成：
  - `content-system/topic-library/topic-library.json`
  - `content-system/index/topics.json`
  - `content-system/configs/writing-config.json`
  - `content-system/configs/system-config.json`
  - `content-system/index/configs.json`
- 实际校验结果：
  - 选题库全文：`99` 条
  - 选题索引：`99` 条
  - 配置索引：`2` 条
  - 当前选题状态统计：`pending 96 / in-progress 1 / completed 2`

## 2026-04-02（更新根目录文档口径，统一到当前真实实现）

### 本轮完成

- 更新 `AI开发手册.md`
  - 补齐当前真实模块现状
  - 明确当前已经接入 OSS
  - 明确历史记录当前是 `localStorage + .local-data + OSS`
- 重写 `历史记录持久化统一处理逻辑.md`
  - 从旧的“本地双保险”口径改为当前真实的“三层持久化”口径
  - 明确 Git 不是数据同步手段，OSS 才是当前跨设备恢复源
- 更新 `OSS云端存储结构设计v1.md`
  - 补充“当前已实现到哪一步”
  - 把已完成项与未完成项区分清楚

### 涉及文件

- `AI开发手册.md`
- `历史记录持久化统一处理逻辑.md`
- `OSS云端存储结构设计v1.md`
- `开发日志.md`

### 验证情况

- 本轮为文档口径更新
- 已按当前代码实现逐项对照，不涉及新的构建变更

## 2026-04-02（补严 Git 推送与拉取规则）

### 本轮完成

- 更新 `AI开发手册.md`
  - 补充了 OSS 接入后的 `git push / git pull` 检查要求
  - 明确 Git 只同步代码，不同步用户真实创作数据
  - 明确 `.env`、AccessKey、OSS 导出 JSON、`.local-data/` 不得进入 Git
- 更新 `历史记录持久化统一处理逻辑.md`
  - 补充拉取后优先检查 OSS 恢复链路的规则
  - 增加“持久化结构变更必须先做迁移方案”的统一要求
  - 明确哪些 key、路径、索引文件不能直接硬改

### 涉及文件

- `AI开发手册.md`
- `历史记录持久化统一处理逻辑.md`
- `开发日志.md`

### 验证情况

- 本轮为文档规则更新
- 已按当前 OSS 实现口径校准，不涉及新的构建变更

## 2026-04-02（改为 Git 跟踪共享运行配置）

### 本轮完成

- 新增根目录共享配置文件 `runtime-config.shared.json`
  - 当前统一保存项目级 `MiniMax / OSS / Doubao` 运行配置
  - 目标是让公司电脑和家里电脑都可以在 `git pull` 后直接运行
- 重写 `server/runtimeConfig.js`
  - 当前优先读取 `runtime-config.shared.json`
  - `.env` 改为本机临时覆盖
  - `API_KEY.md` 仅保留 MiniMax 的兜底读取
- 统一运行时配置入口
  - `server/ossContentStorage.js` 不再自己解析 `.env`
  - `vite.config.js` 现在会统一注入 `MiniMax / Doubao` 共享配置
  - `api/benchmark-transcribe.js` 改为读取共享 Doubao 配置
  - `api/content-draft.js`
  - `api/topic-recommendations.js`
  - `api/benchmark-chat.js`
  - `api/benchmark-analyze.js`
    这些接口的模型读取也统一收口到共享运行配置
- 更新根目录文档
  - `AI开发手册.md`
  - `历史记录持久化统一处理逻辑.md`
  - `OSS云端存储结构设计v1.md`
  - 明确当前私人仓库场景下，允许通过 `runtime-config.shared.json` 跟踪项目级密钥

### 涉及文件

- `runtime-config.shared.json`
- `server/runtimeConfig.js`
- `server/ossContentStorage.js`
- `vite.config.js`
- `api/benchmark-transcribe.js`
- `api/content-draft.js`
- `api/topic-recommendations.js`
- `api/benchmark-chat.js`
- `api/benchmark-analyze.js`
- `AI开发手册.md`
- `历史记录持久化统一处理逻辑.md`
- `OSS云端存储结构设计v1.md`
- `开发日志.md`

### 验证情况

- 已执行：`node -e "import('./server/runtimeConfig.js').then((m) => { console.log(JSON.stringify({ minimax: m.resolveMiniMaxConfig(), doubao: m.resolveDoubaoAsrConfig(), oss: m.resolveAliyunOssConfig() }, null, 2)) })"`
- 共享配置读取结果：
  - `MiniMax`：成功读取
  - `OSS`：成功读取
  - `Doubao`：当前仍为空，保持未配置状态
- 已执行：`node -e "import('./server/ossContentStorage.js').then(async (m) => { const result = await m.runAliyunOssHealthcheck(); console.log(JSON.stringify(result, null, 2)); })"`
- OSS 健康检查结果：通过
- 已执行：`npm run build`
- 构建结果：通过

## 2026-04-02（排版预览切换到统一 HTML 模板渲染）

### 本轮完成

- 新增统一的文章排版 HTML 渲染模块
  - 保留 `draftMarkdown` 作为正文源
  - 统一输出完整预览文档 HTML 和复制用 HTML 片段
  - 统一处理标题区、正文区、正文配图、尾部固定内容
- 将排版预览从页面内 `ReactMarkdown` 直接渲染改为 `iframe srcDoc`
  - 现在 PC / 移动端预览都基于同一份 HTML 成品渲染
  - 切换设备和字号时，会直接重建对应的 HTML 预览结果
- 将“复制微信样式”改为复用同一份 HTML 成品
  - 不再维护单独的隐藏复制 DOM 模板
  - 预览区看到的结构和复制出去的结构保持一致
- 重做正文自动配图的结构锚点逻辑
  - 统一改为固定 `3` 段 section 规则
  - 优先按正文前 `3` 个 `h2 / h3` 小节切段
  - 若不足 `3` 个小节，则按正文段落均分
  - 新写入的图片槽位增加 `sectionOrder`
  - 历史会话若没有 `sectionOrder`，仍按原有 `order` 兼容读取
- 调整素材匹配返回结构
  - `/api/library-assets/match` 的 `slots` 现在会附带 `sectionOrder`

### 涉及文件

- `src/lib/articlePreviewHtml.jsx`
- `src/pages/BenchmarkWorkbenchPage.jsx`
- `server/libraryAssets.js`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过
- 已确认：
  - 排版预览已切换为 `iframe srcDoc` HTML 渲染
  - 复制微信样式已改为复用同一份 HTML 渲染结果

### 当前约束

- 继续保留 `draftMarkdown` 作为唯一正文源
- 当前不新增手动调图位能力
- 当前不把生成后的 HTML 成品写入 OSS

### 下一步

- 可继续按这套 HTML 模板渲染方案迭代排版细节
- 等待用户下一步明确的排版或内容定制需求

## 2026-04-02（新增排版模板调试模块）

### 本轮完成

- 新增独立侧边栏模块 `排版模板`
  - 不再把模板调试塞进原来的 `图片配置`
  - 左侧使用参数面板，右侧使用移动端 `iframe` 所见即所得预览
- 新增本地排版模板配置
  - 新增 `.local-data/article-template-config.json`
  - 通过 `GET /api/article-template-config`、`PUT /api/article-template-config` 读写
  - 明确只做项目本地配置，不进 Git、不进 OSS、不写入运行时共享配置
- 将正式排版预览和微信复制接入模板配置
  - `renderArticlePreviewDocument()` 支持读取统一模板参数
  - 标题、元信息、正文、正文配图、文末区域、颜色系统都改为读模板配置
- 将正文配图的段位顺序接入模板配置
  - 模板模块可调 `正文配图 1/2/3` 分别插在第几段后
  - 自动配图请求会按模板指定的 section 顺序去匹配素材
  - 正式预览与复制结果也会按当前模板顺序渲染
- 增加模板调试专用占位内容
  - 内置一篇示例文章和 3 个正文配图占位块
  - 固定头图、文末文案、二维码、底图在缺失时会显示带标签占位块

### 涉及文件

- `shared/articleTemplateConfig.js`
- `server/articleTemplateConfig.js`
- `api/article-template-config.js`
- `src/lib/articlePreviewHtml.jsx`
- `src/pages/BenchmarkWorkbenchPage.jsx`
- `vite.config.js`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过
- 已确认：
  - 新的模板模块已接入侧边栏
  - 正式排版预览已经读取统一模板配置
  - 自动配图的 section 顺序已受模板配置控制

### 当前约束

- 第一版只提供移动端模板调试画布
- 第一版只做参数面板，不做自由拖拽
- 第一版仍然保留 `draftMarkdown` 作为唯一正文源

## 2026-04-03（收敛图片配置页面 UI）

### 本轮完成

- 优化 `图片配置` 页面视觉结构，整体收成更简洁的配置面板风格
- 调整页面头部
  - 去掉原来偏重的数量胶囊
  - 改成标题 + 一行轻说明
- 调整固定内容列表容器
  - 统一成一个圆角面板
  - 顶部增加轻量区块标题和配置项数量
  - 行与行之间只保留细分割线
- 重做图片槽位行样式
  - 缩小缩略图尺寸与圆角
  - 信息结构统一为：名称 / 类型标签 / 说明 / 文件信息
  - 操作按钮改得更短、更克制
- 重做固定文案行样式
  - 左侧保留名称和说明
  - 右侧改成更规整的输入区 + 底部操作条
  - 弱化多余提示文案

### 涉及文件

- `src/pages/BenchmarkWorkbenchPage.jsx`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 本轮只调整 UI 结构和样式
- 不改固定图片上传、删除、预览逻辑
- 不改接口和数据结构

## 2026-04-03（固定模板排版链路重构 v1）

### 本轮完成

- 将正文生成与审核规则切换到固定模板占位符契约
  - 正文必须包含 `[IMAGE_1]`、`[IMAGE_2]`、`[IMAGE_3]`、`[ENDING]`
  - 审核阶段新增占位符完整性与顺序检查
  - 不再允许正文里输出 `▽` 和固定关注引导语
- 固定模板渲染链路落地到文章预览与微信 HTML
  - 统一使用固定模板结构渲染头图、引导关注图、三段正文配图、结尾区、二维码区和底部动图
  - 正文字号固定为 `17px`
  - 行高固定为 `1.9`
  - 最大宽度固定为 `677px`
- 固定素材配置扩展了新的 `guideFollow` 槽位
  - 缺失素材时自动使用占位图，不留空白
- 内容创作里的 `排版预览` 收敛为固定模板预览
  - 去掉设备切换和字号切换
  - 保留全屏视图、复制微信样式、同步微信草稿
- 删除动态排版模板的主流程入口
  - 侧边栏不再出现 `排版模板`
  - 停用 `/api/article-template-config`
  - 删除 `server/articleTemplateConfig.js`

### 涉及文件

- `src/lib/articlePreviewHtml.jsx`
- `src/pages/BenchmarkWorkbenchPage.jsx`
- `server/contentCreation.js`
- `server/contentRuleProfiles.js`
- `shared/fixedLayoutConfig.js`
- `server/fixedLayoutConfig.js`
- `public/assets/fixed-content/config.json`
- `vite.config.js`
- `开发日志.md`

### 验证情况

- 已执行：`npm run build`
- 结果：通过

### 当前约束

- 本轮只重构固定模板排版链路，不改内容创作整体流程
- 旧的 `ArticleTemplateModuleCanvas` 代码仍保留在文件中，但已经不再出现在实际入口中
